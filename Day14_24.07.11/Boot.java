import java.util.ArrayList;
import java.time.LocalDateTime;

public class Boot {
    private ArrayList<Boot> boote;
    private ArrayList<Person> kunden;

    public Boot bestBoot() {
        for (int j = 0; j < boote.size() ; j++) {
                for(int i = 0; i < )
            }
        }
    }
}
